# 上一年 H 题地面站 GUI 最小实现与逐步调试流程

> 当前方案：地面站香橙派 + HDMI 触摸屏，机载树莓派，全部使用 ROS 2 通信  
> 地面站环境：Ubuntu 22.04 + ROS 2 Humble  
> GUI 推荐：Python + PySide6  
> 原则：只实现题目明确要求；每完成一个功能就单独调试，通过后再继续。  
> 说明：本方案按上一年 H 题功能进行复现；原题硬件描述中提到地面站包含微控制器，纯香橙派方案用于当前 ROS 2 系统开发。

---

## 1. 最小功能

地面站只实现：

1. 在 9×7 方格地图上触摸设置禁飞区。
2. 显示规划出的巡查航线。
3. 一键启动巡查。
4. 巡查中实时显示：
   - 方格代码
   - 动物名称
   - 动物数量
5. 保存本次巡查的识别记录。
6. 巡查结束后：
   - 显示每种动物总数量
   - 能重新查看本次识别记录

不增加：

- 电池和高度显示
- 实时位置动画
- 航线偏差显示
- 进度条
- 识别置信度
- 多次历史任务
- 设置和调试页面

---

## 2. 系统结构

```text
机载树莓派
├── 飞行控制节点
├── 动物识别节点
└── ROS 2 通信
        ↕ Wi-Fi
地面站香橙派
├── ground_station_gui
├── 航线规划
├── 数据保存与统计
└── HDMI 触摸屏
```

不使用地面站 MCU，不使用串口桥。

---

## 3. 节点分工

### 3.1 地面站节点：`ground_station_gui`

负责：

- 显示 GUI。
- 接收触摸操作。
- 设置禁飞区。
- 规划并显示航线。
- 发布航线。
- 发布启动命令。
- 接收动物识别结果。
- 保存记录。
- 统计动物总数。
- 显示最终结果。

### 3.2 机载树莓派节点

负责：

- 接收地面站航线。
- 接收启动命令。
- 执行自主巡查。
- 发布动物识别结果。
- 发布任务完成消息。

---

## 4. 最小 ROS 2 话题

| 话题 | 方向 | 类型 | 内容 |
|---|---|---|---|
| `/planned_route` | 香橙派 → 树莓派 | `std_msgs/msg/UInt8MultiArray` | 航线方格索引 |
| `/mission_start` | 香橙派 → 树莓派 | `std_msgs/msg/Bool` | 启动巡查 |
| `/animal_detection` | 树莓派 → 香橙派 | `std_msgs/msg/String` | `A3B5,虎,2` |
| `/mission_finished` | 树莓派 → 香橙派 | `std_msgs/msg/Bool` | 任务完成 |

不增加其他话题。

---

## 5. GUI 页面

只做 3 个页面，使用 `QStackedWidget` 切换。

| 页面 | 功能 |
|---|---|
| 地图设置页 | 设置禁飞区、显示航线、开始巡查、查看结果 |
| 巡查显示页 | 实时显示最新动物识别结果 |
| 结果页 | 显示动物总数和全部识别记录 |

---

# 6. 地图设置页

## 6.1 最简布局

```text
┌──────────────────────────────────────────────┐
│                 9×7 方格地图                 │
│                                              │
│   B1  □ □ □ □ □ □ □ □ □                    │
│   B2  □ □ □ □ □ □ □ □ □                    │
│   B3  □ □ □ □ □ □ □ □ □     [清空]         │
│   B4  □ □ □ □ □ □ □ □ □     [生成航线]     │
│   B5  □ □ □ □ □ □ □ □ □     [开始巡查]     │
│   B6  □ □ □ □ □ □ □ □ □     [查看结果]     │
│   B7  □ □ □ □ □ □ □ □ □                    │
│       A1 A2 A3 A4 A5 A6 A7 A8 A9            │
│                                              │
│   状态：等待设置                             │
└──────────────────────────────────────────────┘
```

## 6.2 地图实现

使用：

```text
QGraphicsView + QGraphicsScene
```

绘制：

- 63 个方格。
- A1～A9、B1～B7 编号。
- 禁飞区颜色。
- 巡查航线。

方格索引：

```text
index = row × 9 + column
```

数据：

```python
blocked = [False] * 63
route = []
```

## 6.3 禁飞区操作

点击方格：

```text
未选中 → 变灰并设为禁飞区
已选中 → 恢复并取消禁飞区
```

点击“清空”：

```text
blocked 全部清零
清除禁飞区颜色
清除旧航线
```

## 6.4 生成航线

点击“生成航线”：

```text
读取 blocked[]
  ↓
调用 plan_route()
  ↓
得到 route[]
  ↓
在地图上显示航线
```

规划函数：

```python
def plan_route(blocked: list[bool]) -> list[int]:
    ...
```

## 6.5 开始巡查

点击“开始巡查”：

```text
route 为空
  ↓
显示“请先生成航线”

route 非空
  ↓
发布 /planned_route
  ↓
发布 /mission_start
  ↓
切换到巡查页
```

---

# 7. 巡查显示页

## 7.1 最简布局

```text
┌──────────────────────────────────────────────┐
│              任务状态：正在巡查              │
│                                              │
│              方格代码：A3B5                  │
│                                              │
│              动物名称：虎                    │
│                                              │
│              动物数量：2                     │
└──────────────────────────────────────────────┘
```

## 7.2 动物识别消息

树莓派发布：

```text
/animal_detection
```

消息格式：

```text
A3B5,虎,2
```

香橙派解析：

```python
grid_code, animal_name, count = msg.data.split(",")
count = int(count)
```

收到后：

```text
更新显示
  ↓
保存记录
  ↓
累加动物总数
```

数据结构：

```python
records = []

animal_totals = {
    "象": 0,
    "虎": 0,
    "狼": 0,
    "猴": 0,
    "孔雀": 0
}
```

记录格式：

```python
record = {
    "grid": "A3B5",
    "animal": "虎",
    "count": 2
}
```

## 7.3 任务完成

树莓派发布：

```text
/mission_finished
```

收到 `true`：

```text
保存结果
  ↓
更新结果页
  ↓
切换到结果页
```

---

# 8. 结果页

## 8.1 最简布局

```text
┌──────────────────────────────────────────────┐
│ 动物总数                                     │
│ 象：0  虎：3  狼：1  猴：2  孔雀：4         │
│                                              │
│ 识别记录                                     │
│ ┌────┬────────┬────────┬────────┐           │
│ │序号│方格代码│动物名称│动物数量│           │
│ ├────┼────────┼────────┼────────┤           │
│ │ 1  │ A3B5   │ 虎     │ 2      │           │
│ │ 2  │ A6B4   │ 孔雀   │ 1      │           │
│ └────┴────────┴────────┴────────┘           │
│                                              │
│                 [返回地图]                   │
└──────────────────────────────────────────────┘
```

使用：

- 5 个 `QLabel` 显示总数。
- 1 个 `QTableWidget` 显示记录。
- 1 个返回按钮。

## 8.2 保存最近一次结果

保存为：

```text
last_result.json
```

只保存最近一次任务。

程序启动时读取该文件，使任务结束后或程序重启后仍能调出结果。

---

# 9. 开发与逐步调试

每一步都按以下流程：

```text
实现一个功能
  ↓
单独测试
  ↓
确认通过
  ↓
再做下一个功能
```

## 功能 1：GUI 空窗口

实现：

- 创建 PySide6 窗口。
- 设置实际屏幕分辨率。
- 全屏显示。

通过标准：

- HDMI 屏正常显示。
- 触摸能点击按钮。

## 功能 2：三个页面切换

通过标准：

- 地图页、巡查页、结果页可正常切换。
- 程序不闪退。

## 功能 3：绘制 9×7 地图

通过标准：

- 63 个方格数量正确。
- A1～A9、B1～B7 方向正确。
- 地图不超出屏幕。

## 功能 4：禁飞区点击

测试：

- 四个角。
- 中间方格。
- 连续三个方格。

通过标准：

- 点击方格变灰。
- 再次点击取消。
- GUI 与 `blocked[]` 一致。

## 功能 5：清空禁飞区

通过标准：

- 所有方格恢复。
- `blocked[]` 清零。
- 航线清除。

## 功能 6：绘制一条测试线

先连接两个固定方格中心。

通过标准：

- 起点和终点位置正确。

## 功能 7：显示固定测试航线

使用：

```python
route = [0, 1, 2, 11, 20]
```

通过标准：

- 路径顺序正确。
- 横线和竖线正常。

## 功能 8：接入航线规划

测试：

1. 无禁飞区。
2. 一组连续禁飞区。
3. 更换禁飞区位置。

通过标准：

- 航线不经过禁飞区。
- 覆盖全部非禁飞方格。

## 功能 9：测试航线发布

香橙派发布 `/planned_route`。

树莓派检查：

```bash
ros2 topic echo /planned_route
```

通过标准：

- 树莓派能收到完整航线。
- 方格索引顺序正确。

## 功能 10：测试启动命令

香橙派发布 `/mission_start`。

树莓派检查：

```bash
ros2 topic echo /mission_start
```

通过标准：

- 未生成航线时不发布。
- 有航线时只发布一次。

## 功能 11：模拟动物数据

树莓派或终端发送：

```bash
ros2 topic pub --once /animal_detection \
std_msgs/msg/String "{data: 'A3B5,虎,2'}"
```

通过标准：

- 巡查页显示 A3B5、虎、2。

## 功能 12：保存多条记录

依次发送：

```text
A1B1,象,1
A3B5,虎,2
A8B6,孔雀,1
```

通过标准：

- 保存 3 条记录。
- 顺序正确。
- 不相互覆盖。

## 功能 13：动物数量统计

发送：

```text
象 1
虎 2
狼 1
猴 3
孔雀 2
虎 1
```

预期：

```text
象：1
虎：3
狼：1
猴：3
孔雀：2
```

## 功能 14：任务完成

树莓派或终端发送：

```bash
ros2 topic pub --once /mission_finished \
std_msgs/msg/Bool "{data: true}"
```

通过标准：

- 自动进入结果页。
- 动物总数正确。
- 识别记录正确。

## 功能 15：重新调出结果

实现保存和读取：

```text
last_result.json
```

通过标准：

- 关闭并重启 GUI。
- 仍能查看最近一次结果。

## 功能 16：两台设备联调

同时运行：

```text
香橙派：ground_station_gui
树莓派：飞行控制与识别节点
```

通过标准：

- 香橙派发布的航线能被树莓派接收。
- 香橙派发布的启动命令能被树莓派接收。
- 树莓派发布的识别结果能更新 GUI。
- 树莓派发布任务完成后自动进入结果页。

---

# 10. 最小工程结构

## 香橙派

```text
ground_station_ws/
└── src/
    └── ground_station_gui/
        └── ground_station_gui/
            └── main.py
```

## 树莓派

沿用现有机载 ROS 2 工作空间，只需要增加或修改：

- 航线订阅。
- 启动命令订阅。
- 动物识别结果发布。
- 任务完成消息发布。

GUI 初期全部写在 `main.py` 中，不要提前拆分大量文件。

---

# 11. 完整运行流程

```text
香橙派启动 GUI
  ↓
触摸设置禁飞区
  ↓
生成并显示航线
  ↓
发布航线
  ↓
一键启动
  ↓
树莓派执行巡查
  ↓
树莓派发布动物识别结果
  ↓
香橙派实时显示、保存和统计
  ↓
树莓派发布任务完成
  ↓
香橙派保存 last_result.json
  ↓
显示动物总数和全部识别记录
```

---

# 12. 最终验收清单

## 地图设置

- [ ] 显示 9×7 方格地图。
- [ ] 可触摸设置和取消禁飞区。
- [ ] 可清空禁飞区。

## 航线

- [ ] 能生成并显示航线。
- [ ] 航线不经过禁飞区。
- [ ] 覆盖全部非禁飞方格。
- [ ] 树莓派能收到航线。

## 启动

- [ ] 无航线时不能启动。
- [ ] 有航线时可以一键启动。
- [ ] 树莓派能收到启动命令。

## 实时显示

- [ ] 显示方格代码。
- [ ] 显示动物名称。
- [ ] 显示动物数量。

## 保存和统计

- [ ] 每条识别结果均保存。
- [ ] 五种动物总数正确。
- [ ] 任务完成后可查看全部记录。
- [ ] 重启 GUI 后可调出最近一次结果。

全部完成后，即完成香橙派地面站与树莓派机载端的 ROS 2 最小闭环。
