# 工程协作约定

## 运行环境边界

- 实车运行环境是香橙派，ROS2 节点、雷达驱动、Cartographer、STM 串口桥和定点控制最终都应在香橙派侧运行。
- 主机电脑主要用于写代码、整理方案、静态检查、代码阅读和非硬件依赖测试。
- 涉及真实硬件的验证，例如雷达串口识别、雷达启动、STM 串口通信、IMU 数据、底盘运动和实车 TF 检查，应以香橙派侧结果为准。
- 主机电脑上看到的 `/dev/tty*`、`lsusb`、雷达端口、STM 端口不代表实车环境，不能据此修改最终硬件配置。

## 代码与配置原则

- 代码可以在主机电脑侧编写，但启动文件和默认参数要面向香橙派实机部署。
- 雷达后续按 LSN10P 处理，具体串口端口需要在香橙派侧确认后再写死或配置为默认值。
- STM 串口端口也需要在香橙派侧确认，优先使用 udev 固定名称，避免依赖 `/dev/ttyACM0`、`/dev/ttyUSB0` 这类会漂移的名称。
- Cartographer 建图依赖 `/scan`、`/track2vision/imu/data_valid` 和 `base_link -> laser` 静态 TF，实机启动前必须在香橙派侧逐项确认。

## 执行命令注意事项

- 在主机电脑侧可以运行不依赖硬件的检查，例如文件搜索、语法检查、协议单元测试和文档生成。
- 不要把主机电脑侧的雷达启动失败、串口不存在或 USB 设备列表当作实车问题。
- 需要验证硬件时，应明确说明“请在香橙派侧执行”，并给出命令。
- 会让底盘运动的命令必须低速、短时、可急停，并明确提醒先架空轮子或确保安全区域。

## 当前工程重点

- 先冻结 `program.md` 中的工程接口和流程。
- 再实现 ROS2 串口桥 `stm_bridge`。
- 再实现轻量定点控制 `point_controller`。
- 最后用 `car_bringup` 统一香橙派侧启动建图、纯定位和控制流程。

